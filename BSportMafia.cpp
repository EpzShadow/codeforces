#include "bits/stdc++.h"
using namespace std;
int main(){
    float n, k;
    cin >> n >> k;
    float root = sqrt(8*k + 8*n + 9);
    root *= -1.0 /2;
    float x = -1.0/2 * sqrt(8.0*k + 8.0*n + 9.0) + n + 3.0/2;
    int t = x;
    cout << t;
    //cout << ans << endl;

}